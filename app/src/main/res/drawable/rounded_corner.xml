<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Màu nền của RelativeLayout -->
    <corners android:radius="10dp" /> <!-- Độ cong của góc, có thể điều chỉnh theo ý muốn -->
    <stroke
        android:width="2dp"
    android:color="@color/white" />
</shape>